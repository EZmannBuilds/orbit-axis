<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" role="img" aria-labelledby="title desc">
  <title id="title">Orbit axis loading</title>
  <desc id="desc">The Pure Orbit mark with its axis sweeping around a fixed orbit and points.</desc>
  <style>
    .orbit-line-loader__axis {
      transform-box: view-box;
      transform-origin: 64px 64px;
      animation: orbit-line-loader-sweep 1.4s linear infinite;
    }

    @keyframes orbit-line-loader-sweep {
      to { transform: rotate(360deg); }
    }

    @media (prefers-reduced-motion: reduce) {
      .orbit-line-loader__axis { animation: none; }
    }
  </style>
  <circle cx="64" cy="64" r="35" fill="none" stroke="#7657e8" stroke-width="8"/>
  <g class="orbit-line-loader__axis">
    <path d="M37 115 91 13" fill="none" stroke="#7657e8" stroke-width="7" stroke-linecap="round"/>
  </g>
  <circle cx="64" cy="64" r="7" fill="#f4f2fa" stroke="#080a12" stroke-width="2"/>
  <circle cx="89" cy="40" r="8" fill="#b9a7ff"/>
</svg>
